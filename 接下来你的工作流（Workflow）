暂存更改：

Bash
git add .
记录版本：

Bash
git commit -m "说明：新增了某某驱动功能"
同步到云端：

Bash
git push



方式一：打标签 Tag（最推荐，专门用于“版本里程碑”）
Tag 就像是给代码历史记录里的某一个点刻了一座石碑，它是一个只读的快照，非常适合用来标记 v1.0、v1.1 这种稳定版本。

1. 在本地打一个带注释的标签：

Bash
git tag -a v1.0 -m "版本备份：CAN通信与RT-Thread底层配置跑通"
(这里 -a 代表创建一个带注解的标签，-m 后面是你对这个版本的描述)

2. 把这个标签推送到 GitHub：
注意，普通的 git push 是不会推送标签的，你需要专门推送它：

Bash
git push origin v1.0
(如果要一次性把本地所有没推的标签全推上去，可以用 git push origin --tags)

推上去之后，你在 GitHub 网页端的 "Tags" 或 "Releases" 界面，就能看到这个版本了，随时可以打包下载 ZIP 源码。

查看历史推送版本
git log --oneline --all --graph

修改了代码，但还没有执行 git add
撤销所有修改： git checkout -- .

场景	命令
还没 add，想看改了啥	git diff
已经 add，想看准备提交啥	git diff --cached
想看最后一次提交的内容	git show
想看某文件的历史改动详情	git log -p <file>
想看每一行是谁写的	git blame <file>
